# Database (development only - NO PRODUCTION CREDENTIALS)
DATABASE_URL=postgresql://nexalert:nexalert_dev_password@localhost/nexalert_dev
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10

# Security (REPLACE WITH REAL SECRETS IN PRODUCTION)
SECRET_KEY=change-me-in-production

# Environment
ENVIRONMENT=development
